export IDIR="$PD/$1"
export BDIR="$WD/$(echo -n $1 | md5)"
export PKGF="$RD/$1"

export TMPDIR="$BDIR/tmp"
export BD="$BDIR/playground"

for i in "$IDIR" "$BDIR"
do
    source rmmkcd "$i"
done

mkdir "$IDIR/log"
mkdir "$BD"
mkdir "$TMPDIR"

source mkcd "$BDIR/runtime"
